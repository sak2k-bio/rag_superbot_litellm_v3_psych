# FastAPI server dependencies for Render Free Tier (Python 3.13 compatible)
# These versions are guaranteed to have pre-built wheels

fastapi==0.88.0
uvicorn==0.20.0
pydantic==1.10.7
python-multipart==0.0.5
httpx==0.23.3
python-dotenv==1.0.0

# Note: Using older, stable versions that work on all Python versions
# Removed aiohttp to avoid C extension compilation issues