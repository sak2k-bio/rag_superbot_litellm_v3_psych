# FastAPI server dependencies for Render Free Tier (Minimal, Python 3.13 compatible)
# Using the absolute minimum versions that work reliably

fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
httpx==0.25.2
python-dotenv==1.0.0

# Note: These versions are tested to work together on Python 3.13
# Minimal set to avoid dependency conflicts