# FastAPI server dependencies optimized for Render Free Tier
# These versions are known to work without compilation issues

fastapi==0.100.1
uvicorn==0.23.2
pydantic==1.10.12
python-multipart==0.0.6
httpx==0.24.1
aiohttp==3.8.5
python-dotenv==1.0.0

# Alternative if above doesn't work:
# fastapi==0.95.2
# uvicorn==0.22.0
# pydantic==1.10.9