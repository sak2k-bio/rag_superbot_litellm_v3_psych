# FastAPI server dependencies matching the working v2 project
# Using aiohttp (like v2) instead of httpx for 1minAI API calls

fastapi==0.100.1
uvicorn[standard]==0.23.2
pydantic==2.3.0
python-multipart==0.0.6
aiohttp==3.9.1
python-dotenv==1.0.0

# Note: Using aiohttp like the working v2 project
# This is the exact combination that works with your API key