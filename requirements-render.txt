# FastAPI server dependencies for Render Free Tier (Python 3.13 compatible)
# These are the latest versions that work with Python 3.13

fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.9.2
python-multipart==0.0.12
httpx==0.27.2
python-dotenv==1.0.1

# Note: Using latest stable versions that are Python 3.13 compatible
# All packages have pre-built wheels available